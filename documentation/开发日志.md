# 20260215
「做到」
给创建名单页面增加控件。
统一HWND的命名风格。
增加了readmeForCode.md。
针对getCtrlSize的两个返回值的问题，使用tuple来解决，前面的使用则需要一定的时间来消除。
给设置页面的【创建名单】按钮增加收回窗口。
完成了【创建名单】UI。
配置了发布版本的编译选项。

# 20260212
「做到」  
修改设置页面的静态控件的间距。  
纠正buttonStyleTurnOn函数名为moreControlTurnOn。  
增加修改旧名单的按钮。  
通过主接口来分离window命名空间中的WindowAdjuster。
修改设置页面以适应文本。  
增加了一个新的类，叫作Page，打算用这个东西来代替WindowPages里面的那些操作。  
第一个使有Page的应该是名单的创建页面了。  
getWindowStyle函数分给了Style。  
开始创建listModify页面，主要用于修改或创建名单。  

# 20260130
「做到」  
添加提示是否有名单存在。  
添加选择抽取的名单的下拉列表。  
添加创建名单的按钮。当然，没有做对应的处理。  
添加清单文件app.manifest，使用更方便而看起来高级的按钮。  
将WindowPages里面的一些名字是Color却返回brush的修改了名字。

# 20260128
「做到」
在readme里面补充会增加新的功能。
添加了在设置页面显示是否已经选择了抽取名单。

# 20260127
「状态」
很久没有再动这个项目了。现在想回来都不知道从哪里开始了。从今天开始要写开发日志了。  
运行了一下，编译错误，有一个地方的代码是断的，上次应该就是void WindowPages::settingOnCreate()这个函数这里断掉的。  
从AI聊天记录那里发现自己上次是在问怎么处理多个同字体不同大小的问题，这个没有修改，但应该问题不大，如果有幽灵问题再修。  
不知道为什么自己写项目逃不开全局变量。  
「做到」  
对设置页面的文本进行更合理的控件长度处理。  
「做到」
给设置页面又加了些需要返回值的函数，在考虑*要不要给所有的消息处理函数加返回值，不要那么乱*。  
「问题」  
在创建设置页面的欢迎语句的时候，总是有1406L的报错，说是不能创建子控件。
「做到」
在github上新建分支，并push上去作为新的活跃分支。