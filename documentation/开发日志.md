# 20260506
「做到」  
现在修复了不返回图标界面的问题。  
「问题」
1. 重进图标页面不加载图标。（如今的修改方案导致返回抽取页面不正常）
1. 随机数不随机。（原来是没有保存剩余名字的修改）

# 20260502
发现每次开启都会第一个抽出“抽完一轮”是因为没有保存剩下的东西。  
「问题」  
1. 抽取页面与图标页面的转换不对。  

# 20260501
早点把这个程序开发完，早点去做我的win32库，受不了这种难受的库了。  
估计以后要再一次rewrite。  
「问题」  
1. 名单的新建不会自动重启应用（新建了一个windowRestart.ixx解决了）  
1. 密码在不用时不会通过（不是不通过，是程序没有反馈）  
1. 抽取名字出现问题（没有重置scrollNum导致的，已解决）  
1. 数据库中的lists没有得到更新（已解决，是IfOK没有处理好，直接删了）  
1. 每次启动应用抽取到的第一个会是“抽完一轮”（已于20260502处理）  

「做到」  
1. 使密码的检查有用了  

# 20260430
等自己的库开发完成是不理智的。  
目前的主要问题是数据读不上来。  
现在看到自己的文件管理，忽然有点想看看CMake会不会更好一些。  
一番F12发现原来在初始化的时候没有读取数据库。  
sqlite出现语法错误了。  
解决了数据读取的问题，但是发现抽取页面白了。  
确定了，应该是用了PCWSTR导致没有保存c_str()返回的结果，所以得到的是空字符串。  

# 20260424
完善了抽取页面的标题栏。  
完成抽取页面关闭按钮的制作。  

# 20260422
推进项目。  
要是自己直接写个库会好很多。发现在一个假成品库上进行开发是个很累人的事。也可以说热情退散了。  
修复了抽取页面许多按钮无法显示文字的问题（实际上只是换成了静态控件）。  

# 20260414
本来在想要不要同步一下抽取页面的代码的，但是想了下还是算了，毕竟现在这个窗口类算不上说是个库，等我到时候全力实现一个库再说。  
也不知道我实现库的时候会不会出现这个项目的问题，落地能力不大行啊。  
算了，实在是找不到原因，重写吧。  

# 20260413
「问题」  
抽取页面的DrawItem消息没有触发。  
原来只是窗口的父窗口在被初始化之前调用来创建窗口导致的。  
但是控件异常，此外还有重新进入抽取页面还会出现random的错误。  

# 20260406
「做到」  
补充了部分函数的说明。  
「问题」  
保存名单时报出sql语法错误。
「做到」  
解决上面的这个问题。  
「问题」  
密码页面还没有subclass。  
抽取页面没有做好。  

# 20260325
「做到」
修改【创建名单】从新建页面为启用原页面。

# 20260312
「做到」
修复Toggle会出现的圆圈变成刂的问题。注意char赋值给wstring的某个元素没有出现报错或报警。
修复【保存名单进入下一步】刚开始处于启用状态的问题。

# 20260305
「做到」
在WindowAdjuster中增加控件Toggle的大小的处理。
「问题」
sql类的设计思路基本没有了。

# 20260302
「问题」
高看自己的记忆力了，我之前写的Sql是什么东西。
「做到」
密码页面的返回名单页面按钮逻辑处理。
改进页面返回的逻辑，防止每次都是创建页面而不是原页面显示。
取消了sqldefault::SPLIT的使用，总觉得不好。
「将要」
在名单页面添加是否设置为默认名单的选项。

# 20260226
「做到」  
给密码的两个框加边框颜色以示匹配程度。
使用了region宏标记，来让代码更好看。  
加入color来处理颜色，并使用了一些颜色。  
完成密码页面的ui。  

# 20260223
「做到」  
增加对密码重要性的提示。  
增加密码输入框。  
用类的方式来处理控件。  
把id全部放进id.ixx中。  
有几个静态文本不知道为什么会不见，AI说是id消息分流导致的，可能吧，反正加了id后就好了。  

# 20260222
「做到」  
创建名单页面增加enter从名单名字到名单内容的跳转功能。  
修复密码页面的文本颜色。  
增加选择是否要密码的选项，由于radiobutton的可个性化范围小，可能要自绘。  

# 20260220
「做到」  
给isAllWspace设为noexcept。  
在Style里面加成员函数setFont。  

# 20260219
「做到」  
在创建名单页面增加返回按钮。  
添加按tab在创建名单页面从名单名字跳转到名单内容的逻辑。  
处理了ctrl+backspace输入(wchar_t)127的问题。  
在编辑框的逼迫下，开启专门用于子类化的模块。  
关闭了tab长度，发现只不过是增加了一个tab而已。  
使用了commctrl.h的子类化方法，并重新开启了tab长度控制，以前应该是使用子类化后tab跳转的逻辑被我拦截导致tab可用了。  
开始起稿密码页面，同时尝试把文件分散到文件夹中和分开到不同的筛选器中。  

# 20260217
「做到」  
增加【打开源码网站】逻辑。并因此增加了website.ixx。  
修改了readme.md的观感。  
增加了创建名单页面一个【保存名单，进入下一步】按钮，并让其根据其他两个edit是否有内容来启用。  
输入名字的编辑框支持tab，并让tab为八个字宽度。  

# 20260215
「做到」  
给创建名单页面增加控件。  
统一HWND的命名风格。  
增加了readmeForCode.md。  
针对getCtrlSize的两个返回值的问题，使用tuple来解决，前面的使用则需要一定的时间来消除。  
给设置页面的【创建名单】按钮增加收回窗口。  
完成了【创建名单】UI。  
配置了发布版本的编译选项。  

# 20260212
「做到」  
修改设置页面的静态控件的间距。  
纠正buttonStyleTurnOn函数名为moreControlTurnOn。  
增加修改旧名单的按钮。  
通过主接口来分离window命名空间中的WindowAdjuster。
修改设置页面以适应文本。  
增加了一个新的类，叫作Page，打算用这个东西来代替WindowPages里面的那些操作。  
第一个使有Page的应该是名单的创建页面了。  
getWindowStyle函数分给了Style。  
开始创建listModify页面，主要用于修改或创建名单。  

# 20260130
「做到」  
添加提示是否有名单存在。  
添加选择抽取的名单的下拉列表。  
添加创建名单的按钮。当然，没有做对应的处理。  
添加清单文件app.manifest，使用更方便而看起来高级的按钮。  
将WindowPages里面的一些名字是Color却返回brush的修改了名字。

# 20260128
「做到」
在readme里面补充会增加新的功能。
添加了在设置页面显示是否已经选择了抽取名单。

# 20260127
「状态」
很久没有再动这个项目了。现在想回来都不知道从哪里开始了。从今天开始要写开发日志了。  
运行了一下，编译错误，有一个地方的代码是断的，上次应该就是void WindowPages::settingOnCreate()这个函数这里断掉的。  
从AI聊天记录那里发现自己上次是在问怎么处理多个同字体不同大小的问题，这个没有修改，但应该问题不大，如果有幽灵问题再修。  
不知道为什么自己写项目逃不开全局变量。  
「做到」  
对设置页面的文本进行更合理的控件长度处理。  
「做到」
给设置页面又加了些需要返回值的函数，在考虑*要不要给所有的消息处理函数加返回值，不要那么乱*。  
「问题」  
在创建设置页面的欢迎语句的时候，总是有1406L的报错，说是不能创建子控件。
「做到」
在github上新建分支，并push上去作为新的活跃分支。